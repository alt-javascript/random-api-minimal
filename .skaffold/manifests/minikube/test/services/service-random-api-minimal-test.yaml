apiVersion: v1
kind: Service
metadata:
  name: service-random-api-minimal-test
spec:
  selector:
    app: pod-random-api-minimal-test
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80